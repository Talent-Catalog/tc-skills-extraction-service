# Deployment install (Linux/Fargate) - pins torch to the "+cpu" wheel (see
# the torch comment block at the bottom of this file). On macOS this file
# will fail to resolve at the torch line - use requirements-dev.txt for
# local development instead; see that file's header for why.
fastapi[standard]==0.139.0
#(Note that fastapi[standard] includes uvicorn, pydantic, and other dependencies)

spacy==3.8.14  # no Python 3.14 wheel at this pin (3.8.16 has one) - this project targets 3.12, see main.py
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
pytest
numpy==2.5.1

# Added for POST /extract_candidate_occupations (app/services/cv_extraction/):
python-multipart==0.0.32  # FastAPI UploadFile/File(...) form uploads
pyyaml==6.0.3             # reads the vendored schema YAML in cv_extraction/schemas/
anthropic==1.2.0          # Claude API client for Stage 2/4 of the extraction pipeline

# docling==2.124.0 converts uploaded PDFs to Doctags markup (Stage 1 input).
#
# RESOLVED (2026-09-01): the earlier concern here was that installing docling
# unconstrained pulls its own torch dependency, resolving to the full CUDA
# build instead of the "+cpu" build pinned below for sentence-transformers -
# which would repeat the OOM incident documented further down this file.
# Verified for real via `docker build` against this project's own Dockerfile
# (python:3.12-slim, linux/amd64, matching Fargate's architecture): with
# torch==2.8.0+cpu already pinned and --extra-index-url pointing at PyTorch's
# CPU index, pip resolves docling against that same torch==2.8.0+cpu install
# with no conflict (`pip check` clean, no nvidia-*/CUDA packages pulled in).
# The earlier "did not cleanly succeed in a throwaway venv" result was a
# platform-tag artifact of testing on macOS/ARM: torch's "+cpu" wheels are
# only published for linux/win under the manylinux_2_28 tag (macOS torch
# wheels have no "+cpu" suffix - they're CPU/MPS-only by default on PyPI), so
# a Mac-native venv can never find the pinned "+cpu" version at all,
# regardless of docling. Testing must use a linux/amd64 environment (Docker,
# as above, or CI) to mean anything for this dependency.
docling==2.124.0

# sentence-transformers generates the embeddings for the /embeddings endpoint.
# It is pinned (like everything else here) because an earlier unpinned install
# silently picked up new sentence-transformers/torch releases between builds,
# which changed the deployed container's memory footprint enough to make it
# fail to start on its Fargate task (OOM on startup -> ECS deployment rollback).
sentence-transformers==5.7.0

# sentence-transformers runs its models on top of PyTorch (torch): torch is the
# ML framework that provides tensor math and executes a model's layers to turn
# input text into an embedding vector. It is not used directly by our code, but
# it is the biggest dependency in this file by far.
#
# By default "pip install torch" resolves to PyPI (pypi.org, the standard
# public package index pip downloads from), which serves a build of torch
# bundled with the full NVIDIA CUDA runtime - useful for GPU machines, but dead
# weight on our GPU-less Fargate tasks. PyTorch publishes a separate, much
# smaller CPU-only build under its own package index instead of as a separate
# PyPI package, identified by the "+cpu" local version suffix (e.g. 2.8.0+cpu).
#
# --extra-index-url adds that index alongside PyPI (rather than replacing it,
# which is what --index-url would do and would break every other package
# above, since PyTorch's index only hosts torch/torchvision/torchaudio).
# Pinning the exact "+cpu" version is what makes pip fetch the CPU build
# instead of PyPI's default CUDA build: PyPI has no package matching that
# literal version string, so there's no ambiguity about which index it must
# come from.
#
# This pin is also, like spacy above, the other reason this project can't
# run on Python 3.14: torch==2.8.0 has no cp314 wheel (PyTorch's latest,
# 2.13.0, does). Bumping it isn't a small patch jump like spacy's fix -
# verify the OOM-on-Fargate history above still holds before touching this.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu
